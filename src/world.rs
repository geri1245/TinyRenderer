use std::rc::Rc;

use crate::model::InstancedRenderableMesh;

pub struct World {
    // pub instanced_meshes: Vec<InstancedRenderableMesh>,
    // pub instanced_meshes_with_pending_materials: Vec<Rc<InstancedRenderableMesh>>,
}

impl World {
    // pub fn new() -> Self {
    //   World { instanced_meshes: (), instanced_meshes_with_pending_materials: () }
    // }
}
